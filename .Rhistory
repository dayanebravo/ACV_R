# Carregar a biblioteca readxl
library(readxl)
install.packages("readxl")
install.packages("readxl")
library(readxl)
# Ler o arquivo XLSX
caminho_arquivo <- "A.xlsx"
dados <- read_xlsx(caminho_arquivo)
# Carregar a biblioteca readxl
library(readxl)
# Ler o arquivo XLSX
caminho_arquivo <- "A.xlsx"
dados <- read_xlsx(caminho_arquivo)
# Exibir todas as linhas e colunas do arquivo
print(dados)
# Ler o arquivo XLSX
caminho_arquivo <- "A.xlsx"
dados <- read_xlsx(caminho_arquivo)
# Exibir todas as linhas e colunas do arquivo
print(dados)
# Calculando o determinante da matriz
determinante <- det(A)
# Exibir todas as linhas e colunas do arquivo
print(A)
# Exibir todas as linhas e colunas do arquivo
print(A)
# Ler o arquivo XLSX
caminho_arquivo <- "A.xlsx"
A <- read_xlsx(caminho_arquivo)
# Exibir todas as linhas e colunas do arquivo
print(A)
# Verificando se a matriz é quadrada (mesmo número de linhas e colunas)
if (nrow(A) == ncol(A)) {
print("A matriz é quadrada.")
# Calculando o determinante da matriz
determinante <- det(A)
# Verificando se o determinante é diferente de zero
if (determinante != 0) {
print("A matriz possui determinante diferente de zero.")
} else {
print("A matriz possui determinante igual a zero.")
}
} else {
print("A matriz não é quadrada.")
}
print(det(A))
# Ler o arquivo XLSX
caminho_arquivo <- "A.xlsx"
df <- read_xlsx(caminho_arquivo)
A <- as.matrix(df)
# Exibir todas as linhas e colunas do arquivo
print(A)
# Ler o arquivo XLSX
caminho_arquivo <- "A.xlsx"
dados <- read_xlsx(caminho_arquivo)
A <- as.matrix(dados)
# Exibir todas as linhas e colunas do arquivo
print(A)
# Verificando se a matriz é quadrada (mesmo número de linhas e colunas)
if (nrow(A) == ncol(A)) {
print("A matriz é quadrada.")
# Calculando o determinante da matriz
determinante <- det(A)
# Verificando se o determinante é diferente de zero
if (determinante != 0) {
print("A matriz possui determinante diferente de zero.")
} else {
print("A matriz possui determinante igual a zero.")
}
} else {
print("A matriz não é quadrada.")
}
print(det(A))
# Ler o arquivo XLSX
caminho_A <- "A.xlsx"
dados_A <- read_xlsx(caminho_A)
A <- as.matrix(dados_A)
caminho_B <- "B.xlsx"
dados_B <- read_xlsx(caminho_B)
B <- as.matrix(dados_B)
# Exibir todas as linhas e colunas do arquivo
print(A)
# Verificando se a matriz é quadrada (mesmo número de linhas e colunas)
if (nrow(A) == ncol(A)) {
print("A matriz é quadrada.")
# Calculando o determinante da matriz
determinante <- det(A)
# Verificando se o determinante é diferente de zero
if (determinante != 0) {
print("A matriz possui determinante diferente de zero.")
} else {
print("A matriz possui determinante igual a zero.")
}
} else {
print("A matriz não é quadrada.")
}
# Multiplicando as matrizes A e B
resultado <- A %*% B
# Imprimindo o resultado
print(resultado)
